numpy
sounddevice   # real-time duplex audio via PortAudio
websockets    # browser frontend (python main.py --web)
bleak         # Bluetooth LE: --ble, --scan, diagnose.py

# No fusion library (e.g. imufusion) is needed: the BNO08x fuses on-chip
# and sends finished orientation.
